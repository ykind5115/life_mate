chore(repo): 重组目录结构，文档归入 docs/，Docker 配置归入 devops/

原先把设计文档与 Docker 配置平铺在项目根目录，随文档数量增长
已难以区分「文档」「环境配置」「待建代码」三类内容。

调整后：

  docs/       设计文档，按依赖顺序编号 01~07
  docs/archive/  已弃用文档，仅作历史留档
  devops/     环境与运维配置（Postgres 初始化脚本）
  src/        （预留）TypeScript 源码
  根目录       仅保留必须在这一层的配置

文件名一并改为 ASCII 短横线命名，原因是中文文件名在 git 中
显示为八进制转义（\346\225\260...），grep / cat / URL 中
均需额外转义。文档内容保持中文不变，身份以文件内标题为准。

docker-compose.yml 保留在根目录，不随 Docker 相关文件移入
devops/：Compose 自动读取的 .env 位置只取决于 compose 文件
所在目录，移入子目录会使「任意目录下执行」变为「必须先 cd」。

本提交为纯重命名，文件内容逐字节未变。
