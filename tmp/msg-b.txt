docs(env): 环境手册随目录重组更新路径并消除配置副本

同步上一提交的目录调整：

- docker/postgres/init → devops/postgres/init
- 目录结构图由「建议新建」改为「当前仓库结构 + 核对命令」，
  因为这些文件已在仓库中，原表述会让读者重复创建

同时修复一处内容漂移：手册内嵌了一份 docker-compose.yml 副本，
已与实际文件不一致（缺 DTYPE、镜像标签仍为旧的 1.5、
挂载路径未更新）。副本是这类文档的常见腐化点，因此改为
「以仓库文件为准 + 核对清单 + docker compose config 校验」，
不再复制全文。

07-git-conventions.md 中示例提交信息里的路径一并更新。
